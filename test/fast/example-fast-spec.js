describe("cats-milestone-by-release test set", function() {
    // it("should have written tests",function(){
    //
    //     expect(Ext.Date.format(new Date(),'Y')).toEqual('2017');
    // });
    //
    // it('should render the app', function() {
    //     var app = Rally.test.Harness.launchApp("cats-milestone-by-release");
    //     expect(app.getEl()).toBeDefined();
    // });
    //
    // it('should render a message if the app is not on a release scoped dashboard', function() {
    //     var app = Rally.test.Harness.launchApp("cats-milestone-by-release");
    //     expect(app.down('#appMessage')).toBeDefined();
    //     expect(app.down('#appMessage').getEl().getHTML()).toMatch(/This app is designed to run on a release scoped dashboard/);
    // });

    it('should get the milestones by feature when given a hierarchy of portfolio items', function(){
      // var app = Rally.test.Harness.launchApp("cats-milestone-by-release");
      //
      // //We can do this becuase the app isn't scoped
      // app.portofolioItemTypes = ["fred","wilma","barney"];
      // var portfolioTestHash = {
      //    "fred": {},
      //    "wilma": {},
      //    "barney": {}
      // };
      // var milestones = app._getMilestonesForFeature(123, portfolioTestHash);
      //
    });

});
